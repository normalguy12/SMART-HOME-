const Client = require('../client');

const client = new Client(
  'uniontownlabs',
  'a36b8e8310994a60860fbd70d214a824',
  {
    success: () => client.Feeds.getAllFeeds().then(() => console.log()),
    failure: console.error
  }
);
